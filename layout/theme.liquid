<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
{% render 'visually_io_sdk' %}

	<!-- Added by AVADA SEO Suite -->
	{% include 'avada-seo' %}
	<!-- /Added by AVADA SEO Suite -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="">
    <link rel="canonical" href="{{ canonical_url }}">
    <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,500;0,600;1,500;1,600&family=Montserrat:wght@400;500;600;700&family=DM+Mono:wght@400;500&display=swap">

    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}
    {%- unless settings.type_header_font.system? and settings.type_body_font.system? and settings.type_accent_font.system? -%}
      <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
    {%- endunless -%}

    <title>
      {%- if current_tags %}{{ current_tags | join: ', ' }} {% endif -%} {{ page_title }}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} | {{ shop.name }}{% endunless -%}
    </title>

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    {% render 'meta-tags' %}
    
    <script src="{{ 'constants.js' | asset_url }}" defer="defer"></script>
    <script src="{{ 'pubsub.js' | asset_url }}" defer="defer"></script>
    <script src="{{ 'global.js' | asset_url }}" defer="defer"></script>
    {{ content_for_header }}

    {% render 'global-theme-styles' %}
    <style>
      :root {
        --font-heading-family: 'Cormorant Garamond', Georgia, serif;
        --font-heading-weight: 500;
        --font-body-family: Montserrat, sans-serif;
        --font-body-style: normal;
        --font-body-weight: 400;
        --font-accent-family: Montserrat, sans-serif;
        --font-mono-family: 'DM Mono', 'Courier New', monospace;
      }
    </style>

    {{ 'base.css' | asset_url | stylesheet_tag }}
    {{ 'custom-styles.css' | asset_url | stylesheet_tag }}
    {%- if settings.predictive_search_enabled -%}
      <link rel="stylesheet" href="{{ 'component-predictive-search.css' | asset_url }}" media="print" onload="this.media='all'">
      <script src="{{ 'predictive-search.js' | asset_url }}" defer="defer"></script>
    {%- endif -%}

    <script>
    document.documentElement.className = document.documentElement.className.replace('no-js', 'js');
    if (Shopify.designMode) {
      document.documentElement.classList.add('shopify-design-mode');
    }
    (() => {
      const updatePreviewBarState = () => {
        document.documentElement.classList.toggle(
          'shopify-preview-bar-active',
          Boolean(document.getElementById('PBarNextFrameWrapper'))
        );
      };
      updatePreviewBarState();
      new MutationObserver(updatePreviewBarState).observe(document.documentElement, {
        childList: true,
        subtree: true
      });
    })();
    </script>
    {%- if request.design_mode -%}
    <script src="{{ 'theme-editor.js' | asset_url }}" defer="defer"></script>
    {%- endif -%}
  <meta name="p:domain_verify" content="283171205048db2681129baec0f74397"/>
  </head>

  <body class="body lx-template--{{ template.name }} lx-settings-sliders--{{ settings.carousel_width }} lx-settings-sliders--av-{{ settings.carousel_arrow_visibility }}">
    <a class="skip-to-content-link button visually-hidden" href="#MainContent">
      {{ "accessibility.skip_to_text" | t }}
    </a>

    {%- if settings.cart_type == 'drawer' -%}
      {%- render 'cart-drawer' -%}
    {%- endif -%}   

    <div class="header-group">
    {% sections 'header-group' %}
    </div>

    {%- if settings.cart_type == 'notification' -%}
    {%- render 'cart-popup' -%}
    {%- endif -%} 
    
    <main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">
      {{ content_for_layout }}
    </main>
    
    {% sections 'footer-group' %}
    <ul hidden>
      <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>
      <li id="a11y-new-window-message">{{ 'accessibility.link_messages.new_window' | t }}</li>
    </ul>

    {% render 'internationalization-global' %}

    <script>
      window.shopUrl = '{{ request.origin }}';
      window.routes = {
        cart_add_url: '{{ routes.cart_add_url }}',
        cart_change_url: '{{ routes.cart_change_url }}',
        cart_update_url: '{{ routes.cart_update_url }}',
        cart_url: '{{ routes.cart_url }}',
        predictive_search_url: '{{ routes.predictive_search_url }}'
      };

      window.cartStrings = {
        error: `{{ 'sections.cart.cart_error' | t }}`,
        quantityError: `{{ 'sections.cart.cart_quantity_error_html' | t: quantity: '[quantity]' }}`
      }

      window.variantStrings = {
        addToCart: `{{ 'products.product.add_to_cart' | t }}`,
        preOrder: `{{ 'products.product.pre_order' | t }}`,
        soldOut: `{{ 'products.product.sold_out' | t }}`,
        unavailable: `{{ 'products.product.unavailable' | t }}`,
        unavailable_with_option: `{{ 'products.product.value_unavailable' | t: option_value: '[value]' }}`,
      }

      window.accessibilityStrings = {
        imageAvailable: `{{ 'products.product.media.image_available' | t: index: '[index]' }}`
      }
    </script>

{% if settings.enable_font_debug_tooltip %}
  <div id="font-debug-tooltip" style="
    position: fixed;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    background: rgba(0,0,0,0.85);
    color: #fff;
    font-family: monospace;
    font-size: 12px;
    padding: 6px 12px;
    border-radius: 6px;
    pointer-events: none;
    z-index: 999999;
    white-space: nowrap;
    opacity: 0;
    transition: opacity 0.15s;
  "></div>
  <script>
    (function() {
      var tip = document.getElementById('font-debug-tooltip');
      var hideTimer;
      document.addEventListener('mouseover', function(e) {
        clearTimeout(hideTimer);
        var el = e.target;
        if (el === tip) return;
        var style = window.getComputedStyle(el);
        var family = style.fontFamily;
        var weight = style.fontWeight;
        var size = style.fontSize;
        var tag = el.tagName.toLowerCase();
        tip.textContent = tag + ' -> ' + family + '  |  ' + weight + '  |  ' + size;
        tip.style.opacity = '1';
      });
      document.addEventListener('mouseout', function() {
        hideTimer = setTimeout(function() { tip.style.opacity = '0'; }, 200);
      });
    })();
  </script>
{% endif %}
</body>
</html>
