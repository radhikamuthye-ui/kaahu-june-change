<style>
.gift-card-placeholder {
  position: relative;
  width: 100%;
  height: 100%;
  min-height: 100px;
  display: flex;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  padding: 1rem;
}

.gift-card-placeholder--card {
  position: relative;
  background: #eeeeee;
  border-radius: 8px;
  width: 100%;
  max-width: 600px;
  height: 0;
  padding-bottom: 58%;
  text-align: center;  
}

.gift-card-placeholder--card span {
  text-align: center;
  font-size: 1.2rem;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%,-50%);
  opacity: 0.6;
  line-height: 1.25;
}

.gift-card-placeholder--card img {
  position: absolute;
  width: 180px;
  height: auto;
  top: 50%;
  left: 50%;
  transform: translate(-50%,-50%);
  opacity: 1;
}
</style>
<div class="gift-card-placeholder">
<div class="gift-card-placeholder--card">
{% if settings.logo_main != blank %}
{{ settings.logo_main | image_url: width: 800 | image_tag }}
{% else %}
<span>{{ shop.name }}</span>
{% endif %}
</div>
</div>